using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Used for commands sent from player to game state
// Mouse or touch input should interact with this invariantly
// Typical pattern is input -> Get Object from game state -> PlayerControls -> Propose change to game state
public class PlayerControls : MonoBehaviour
{
    //TODOs:
    //place object
    //move object
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
